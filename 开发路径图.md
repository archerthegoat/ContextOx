# 开发路径图

本文件记录人类认可的 AlphaOx 开发方向，不作为自动维护的项目状态表。

任何新增、删除、改写、移动、重命名或替换都必须先展示精确内容或差异，并取得人类对该项变更的明确批准。代码、测试、任务结果和 Agent 推断不得自动改写本文件。

## 项目愿景

AlphaOx 是一个基于 Pi Agent 源码二次开发、可通过 Docker 部署、面向公司内部多用户使用的通用分析 Agent。

它通过 Web 分析工作台接收自然语言问题，连接经过授权的数据库、API、文件和企业知识资源，生成可验证的分析计划，通过确定性执行层取得数据与文字证据，并输出图表、表格、解释和可持续保存的分析报告。

AlphaOx 不以某个行业、数据库结构或单一 BI 场景作为核心假设。项目的可复用价值是统一的上下文接入协议、流程控制器、权限边界、分析计划、证据封装和 Pi Agent 运行时。

## 已确认的设计基线

- 直接 fork `earendil-works/pi`，在 Pi monorepo 内进行源码级二次开发。
- 保留官方 Pi 作为 `upstream`，尽量避免对上游 `agent`、`ai` 和其他核心包进行产品专属修改。
- 所有新增产品代码使用 TypeScript/Node，不引入 Python 运行时。
- 首版只新增 `semantic-agent` 和 `semantic-web` 两个产品 package。
- `semantic-agent` 内部包含 Pi 接入、流程控制、上下文、连接器、权限、会话、记忆、报告、Trace 和服务端 API。
- `semantic-web` 提供对话、图表、表格、报告、会话、来源和 Trace 界面。
- 通过 Docker 交付可运行的内部应用；开发环境优先使用 Docker Compose，生产编排在后续决策门确定。
- 多用户隔离覆盖身份、会话所有权、记忆范围、数据源权限、语义资产权限、报告所有权、缓存、预算和审计。
- 模型不得生成或直接执行任意 SQL、JavaScript、TypeScript、Shell 或其他代码。
- 模型只能提出符合机器契约的 Analysis Plan 和 Query Plan。
- 权限、关系、粒度、成本、超时、新鲜度和隐私由确定性代码强制执行。
- 结构化数据库和企业文字知识都属于分析上下文。
- 向量检索只作为授权后上下文发现的可选能力，不作为指标定义或数值查询的权威来源。
- 原始企业数据和第三方参考数据不提交 Git，也不默认打入产品镜像。
- CI 使用项目自行生成的确定性小数据和确定性业务文档。
- Olist 与 AdventureWorks 作为外部参考数据源和 Source Binding 研究对象，在架构报告中记录来源、版本、许可证、适用边界和实测发现。

## 核心开放契约

### Source Connector

统一描述数据库、API、文件和其他结构化来源如何进行连接测试、结构发现、能力声明、只读执行、取消和健康检查。

### Source Snapshot

保存某一时点发现的数据库、Schema、表、字段、类型、主外键、数据量、新鲜度、方言能力和结构指纹。

### Source Binding

保存公司业务概念、指标、维度、关系、时间口径与具体物理字段之间的映射。

Source Binding 可以由 Agent 根据 Schema、文档和样本提出草稿，但关键口径必须经过人类审核后才能发布。

### Context Pack

统一封装结构化数据上下文、业务术语、文字知识、权限、来源、版本、生效时间、新鲜度和示例问题。

Context Pack 的格式必须可版本化、可验证、可导入导出，并允许公司在不修改 Agent 内核的情况下新增自己的上下文。

### Analysis Plan

描述一次完整分析需要执行的步骤，包括结构化查询、知识检索、计算、比较、归因、可视化和报告组织。

### Query Plan

Analysis Plan 中专门用于结构化数据查询的受约束步骤。

Query Plan 不包含模型可任意编写的 SQL，而是引用经过发布的来源、资产、指标、维度、时间、过滤、排序和限制。

### Evidence Envelope

统一封装数据库结果、文字证据、来源、版本、`as_of`、新鲜度、权限范围、运行状态、警告和 Trace 引用。

## 当前开发路径

### 路径一：Fork、治理与架构基线

- 创建 AlphaOx 的 Pi 官方 fork，并配置 `origin` 与 `upstream`。
- 保留上游根目录 `AGENTS.md`，不覆盖 Pi 的工程规则。
- 建立路线图集成分支和串行子分支工作流。
- 写入完整的架构与验收报告。
- 使用 ArchScribe 生成可编辑的正式架构图和预览图。
- 记录目标、事实、假设、约束、替代方案、取舍、失败模式和未决风险。
- 记录完整的 Docker、权限、数据、会话、记忆、知识和报告边界。
- 写入完整的内置 Browser 验收清单。
- 未通过人类架构决策门前，不进入功能实现。

### 路径二：Analysis Context 标准与接入流程

- 定义 Source Connector、Source Snapshot、Source Binding 和 Context Pack 的 JSON Schema。
- 定义结构化数据、文字知识、业务术语、权限、来源和版本的统一标识方式。
- 实现数据库 Schema 自动发现和规范化。
- 实现业务文档、数据字典和知识资源的统一描述。
- 实现 Agent 建议、人类审核、版本发布和回滚状态。
- 实现精确匹配、别名匹配和可选向量检索之间的边界。
- 设计 OpenMetadata、dbt 和 MCP 的可选兼容接口，但不把外部平台设为首版强制依赖。

### 路径三：确定性计划、执行与证据内核

- 定义 Analysis Plan、Query Plan、Execution Context、Result Envelope 和 Evidence Envelope。
- 实现权限前置、关系验证、粒度验证、成本限制和只读执行。
- 实现参数化查询编译，禁止模型直接生成和执行任意 SQL。
- 实现超时、取消、并发限制、循环预算和部分失败处理。
- 实现数据新鲜度、隐私变换、结果最小化和来源证明。
- 实现结构化查询、知识检索和混合分析步骤。
- 实现 `complete`、`partial`、`blocked` 和 `clarification_required` 状态。
- 建立不依赖真实模型的确定性契约测试。

### 路径四：Pi Agent 接入与流程控制

- 在 fork 内通过 Pi Agent Core 建立 AlphaOx Agent。
- 只注册 AlphaOx 的受控分析工具，不开放编码类内置工具。
- 实现问题识别、上下文发现、澄清、计划生成、执行、继续分析和报告生成。
- 使用 Pi 事件流生成统一 Trace。
- 实现会话上下文、用户偏好记忆和组织共享知识的独立作用域。
- 实现模型提供商不可用、工具失败、循环耗尽和上下文冲突的回归测试。
- 保证模型更换不会改变确定性计划和执行契约。

### 路径五：多用户 Web 分析工作台与 Docker 应用

- 建立 `semantic-agent` 后端服务和 `semantic-web` 前端。
- 提供登录适配、用户、组织、角色、会话、消息、记忆、报告和审计数据模型。
- 提供对话、KPI、图表、表格、报告、来源和 Trace 交互。
- 前端只渲染受约束的结构化展示块，不执行模型生成的任意前端代码。
- 建立开发身份适配器和生产 OIDC 接口。
- 建立 Docker Compose 开发与演示环境。
- 密钥、凭据和原始数据只通过运行时配置、Secret 或 Volume 注入。
- 完成桌面端、移动端、键盘、焦点、滚动、加载、空、部分、错误和过期状态验收。

### 路径六：确定性 CI 与外部参考数据源

- 生成项目自有、固定种子、可公开分发的确定性小数据。
- 使用两套不同物理 Schema 表达相同业务事实，验证 Source Binding 的通用性。
- 提供确定性的业务术语、退款政策、促销日历、新鲜度规则和区域权限文档。
- 建立纯结构化、纯知识检索和混合分析的 Golden Questions。
- 固化预期 Analysis Plan、Query Plan、证据和结果。
- 在架构报告中维护 Source Finding 01：Olist。
- 在架构报告中维护 Source Finding 02：AdventureWorks。
- Olist 只作为可选本地开发参考，不进入 CI，不默认随 GitHub 或商业 Docker 镜像分发。
- AdventureWorks 作为 MIT SQL Server 参考数据源，具体版本和下载方式在独立实现门固定。
- 外部参考数据的下载、校验、导入和清理必须与正常用户查询流程分离。

### 路径七：工程化、开放扩展与生产决策门

- 完成日志脱敏、审计、健康检查、指标、Trace 和故障诊断。
- 完成配置、Schema、Context Pack 和数据库迁移及回滚方案。
- 完成 Source Connector 和 Context Pack 扩展开发说明。
- 完成上游 Pi 同步、冲突处理和版本升级流程。
- 完成依赖锁定、供应链检查、镜像扫描和发布前验证。
- 完成真实企业数据源、生产 SSO、对象存储和部署平台的独立评估。
- 在独立批准前不连接真实企业数据，不部署生产，不开放写回能力。
- 全部路径点通过集成验证和对抗审查后，再由人类决定是否合并到 `main`。

## 长期方向

- 支持更多数据库、文件格式、业务 API 和企业知识平台。
- 支持公司自定义 Context Pack、Connector 和分析算子。
- 支持批准后的向量检索、全文检索和混合检索。
- 支持复杂指标、跨来源分析、多阶段归因、预测和异常检测。
- 支持组织级数据目录、血缘、质量和治理平台集成。
- 支持企业部署、水平扩展、队列、缓存和长任务。
- 支持经过独立安全与权限批准的数据导出和业务写回。
- 支持将授权后的 Context Resources 和 Tools 通过开放协议提供给其他 Agent。

## 当前明确不做

- 不承诺任意数据库零配置、零审核即可得到正确业务口径。
- 不把字段名推断或模型生成内容直接视为已批准事实。
- 不允许模型生成或执行任意 SQL、代码、Shell 或数据写入。
- 不把向量检索作为指标、权限或数值查询的唯一保证。
- 不把原始企业数据、凭据、用户历史或第三方完整数据提交 Git。
- 不默认把 Olist 数据打入公开或商业 Docker 镜像。
- 不在首版连接真实企业生产数据库。
- 不在未批准时推送、创建 Pull Request、部署、发布或写回外部系统。

## 人工决策门

每个开发路径点必须从当前路线图集成分支创建独立子分支。

子分支只能实现一个已批准路径点，并完成对应的契约检查、测试、视觉验收和对抗审查。通过后只能合并回路线图集成分支。

所有路径点完成后，必须在路线图集成分支运行完整集成门，并向人类展示：

- 最终差异
- 测试与验收证据
- 架构与安全审查结果
- 外部数据和许可证状态
- 未解决风险
- 迁移与回滚边界

只有在人类通过最终 Decision Gate 后，才能决定是否合并到 `main`。

本路线图不授权推送、Pull Request、部署、发布、生产切换、真实数据访问、破坏性迁移或外部写回；这些操作必须分别获得明确授权。
