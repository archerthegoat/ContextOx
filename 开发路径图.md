# 开发路径图

本文件记录人类认可的 ContextOx（数契，工作品牌）开发方向，不作为自动维护的项目状态表。

任何新增、删除、改写、移动、重命名或替换都必须先展示精确内容或差异，并取得人类对该项变更的明确批准。代码、测试、任务结果和 Agent 推断不得自动改写本文件。

## 项目愿景

ContextOx（数契）是一个从组织目标和全局影响出发，设计、建立并持续演进业务规范的执行 Agent。即使没有现成适用规范，它也应自主设计规范结构和候选规则、查证事实、发现冲突、找到责任人、发起并追踪补证、准备业务裁决，把模糊目标转成可执行、可追溯、可复用、可扩展的规范契约。

ContextOx 的首要问题不是“如何让模型更快回答数据问题”，而是“一个会影响数据、报表、模型、Agent 和业务流程的定义，凭什么成立、由谁决定、何时生效、如何安全落地、何时需要再次改变”。

Agent 在授权范围内承担下一步选择、查证、补证协调、等待、恢复、重规划、影响模拟和契约准备；人提供目标、必要事实与授权，并保留业务裁决、例外接受、最终批准和高风险发布权。业务推进不依赖用户逐步点击。

首个验证入口面向专业用户发起、共 2–5 人参与的团队自助模式。先用自有确定性电商测试数据与明确标注的模拟业务任务，面向已有购买记录的客户，验证“从零建立电商高潜客户识别与运营优先级规范”，贯通指标、数据与业务规则；高潜是待业务裁决的运营定义，不等于已验证的未来购买概率。演示中的角色扮演不算真实团队验证，模拟需求不算客户需求证据。完成七分钟功能闭环后，沙箱发布和真实团队试点分别过门；ICP、付费意愿、商业模式和海外路径仍需真实客户证据确认。

## 已确认的产品基线

- 业务定义、证据、争议、映射、责任、时间、例外、决策和结果是可治理的企业资产。
- Mission 是 Agent 持续推进的第一等执行对象；Case（现候选名 Definition Change Case）保存规范创建或修订的业务事实、方案、证据、争议和决策；Agent Run 是一次有界模型/工具执行。对话和 Session 不是领域状态源，最终公共对象名仍待契约门。
- Business Definition Contract 是批准后、机器可验证的上层契约，不替代所有术语表、指标层或数据契约格式。
- 记录时间与业务有效时间必须分离；历史版本不可原地覆写。
- Agent 生成内容始终区分来源事实、人工陈述、机器推断、未知、冲突和过期。
- Agent 只能在代码和策略授予的 capability 内行动；prompt 不能替代服务端授权。
- 关键定义、例外和高风险发布必须经过有权人明确批准。
- 发布成功必须以目标系统 readback 验证；部分失败、陈旧、阻塞和回滚失败不得包装成成功。
- 产品优先连接既有目录、指标、身份、协作、策略和数据平台，不从零重建通用数据栈。
- 首个演示使用自有 fixture 的 3–4 类资料：原始样例表与结构、字段说明、模拟业务目标与约束、角色与授权依据；资料类别不等于外部连接器数量。交付首份 Contract v1、规则与责任说明、字段映射、可执行检查、样例/反例、只读影响说明和发布提案；不要求已有 Contract，也不伪造 v0。
- 指标、数据和业务规范在同一客户识别案例中贯通：指标回答如何衡量，数据规范约束客户/订单粒度、关联、时间和质量，业务规范明确适用条件、职责、例外、批准与复审。电商只是验证领域，不成为产品内核的硬编码前提。
- 自有测试事实、模拟需求/角色、真实来源事实、人工陈述和 Agent 候选分别标注；既有指标定义或完整 Binding 不作为从零设计的现成答案输入，验收断言留在评估端。
- 工程 fixture 回放、真实模型设计能力、人工验收、沙箱效果和客户价值分别验证；fake 回放不能报告完整 MVP 功能通过。Olist 作为客户识别场景的真实数据验证候选，其具体版本、许可、用途、下载、导入与传播仍需独立确认，默认不进入 CI、安装包、镜像或商业演示。
- 业务上下文图谱是可重建索引，不是另一套业务真相；关系必须保留来源、版本、时间、权限和确认状态，使用频率与模型置信度不能代替正式授权。
- 不以 ChatBI、自然语言 SQL、低代码画布、多 Agent 数量或全自动治理作为首要价值。
- 没有客户基线和测量设计时，不宣称效率提升或业务因果收益。

## 工程与治理基线

- 首个最小闭环由 ContextOx 应用作为唯一产品入口：通过 ContextOx 自有命令启动，并在应用内部经薄适配复用 Pi Coding Agent runtime/TUI 与 Pi Agent Core；不把 ContextOx 做成 Pi role，也不依赖项目级 `.pi` extension 启动产品。
- 首版只验证单机、单发起人、单个明确批准者和一个自有 fixture 场景。TUI 与 Pi Session 只承载交互和运行痕迹，不是 Mission、审批、决定或 Contract 的业务状态源。
- ContextOx 自有持久化保存冻结提案、审批状态、决定和 ContractVersion；首个实现优先使用 Node 内置 SQLite 能力，不新增数据库或工作流依赖。
- 首个闭环不把 ContextOx 领域规则写入 Pi Agent Core、TUI 或 Coding Agent。优先使用公开 SDK；只有静态 Pi identity 被实测证明阻塞 ContextOx 产品身份时，才允许在 Coding Agent 增加一个最小、通用、可选且保持 Pi 默认行为的 identity 注入接缝；其他内核改动仍须另行批准。
- Web、API、Worker、PostgreSQL、DBOS、Temporal、React Flow 和多用户身份系统均不是首个闭环前置条件；仅在 TUI 闭环暴露出明确需求后重新进入架构门。
- 当前 `semantic-agent` 的确定性契约、证据、preflight、RuntimeHost、受控工具和 Pi adapter 是复用输入，不自动成为新产品最终架构。
- ContextOx 领域核心拥有自己的 command、event、state、error 和审计契约，不向 public API 泄漏 Pi 或其他第三方内部类型。
- 当前先在本仓建立 `packages/contextox` 的独立应用边界与 Pi 薄适配；长期是否拆为独立 ContextOx app repo、建立专用 Pi fork、迁移既有包名或升级版本继续按路径七分别批准。
- 身份、权限、数据最小化、secret、策略、审计、持久化、并发、备份、恢复和部署模式在架构报告中冻结，不能只由 prompt 或 UI 约定。
- 原始企业数据、凭据、用户历史、broker/provider 私有数据和第三方受限数据不得提交 Git 或默认进入镜像、日志、trace 和模型上下文。
- 自动化验证、Agent/Browser QA 和人类验收分别记录；人类未明确记录 `PASS` 时保持 `PENDING` 或 `NOT RUN`。
- 竞品公开资料只能证明已披露能力，不能证明其私有功能缺失；市场差异必须通过演示、RFP、客户访谈或试点验证。
- 任何新增依赖都需精确锁版、release/license/install-script/依赖树和供应链审查，并有退出与回滚边界。

## 核心开放契约

以下是建模输入；字段、接口和状态转换须在架构报告中逐项确认，路线图批准不等于接口冻结。

### Mission

描述业务目标、范围、约束、成功条件、授权、预算、责任人、当前判断、下一次唤醒和停止条件。支持观察、行动、等待、恢复与重规划，不把可视化阶段硬编码成必须由人逐步推进的流程。

### Agent Run

描述一次有界模型/工具执行，包含输入上下文、工具权限、预算、输出、错误、取消和 Trace。通过 Pi 薄适配器执行，不自行拥有业务批准权或持久任务状态。

### 业务上下文索引与 Context Pack

索引连接目标、定义、证据、责任人、决策和下游使用关系；Context Pack 按 Mission 提供有权限、可追溯的事实、冲突、缺口和新鲜度。索引可重建，推断关系保留候选状态；撤权、删除和过期必须影响检索、缓存及关键动作前的再校验。

### Definition Change Case

描述一次规范创建、澄清、修订以及后续受控发布、观察、回滚或关闭的生命周期，包含目标、范围、状态、证据、人员、方案、争议、任务、影响、批准和结果。合法创建不强制旧版本，修订必须绑定明确基线；尚不能确认是否存在适用规范时继续查证，不把未找到或无权限当成不存在。

### Business Definition Contract

保存被批准的规范版本、目标与范围、定义与规则、职责、包含/排除、Owner、证据、记录时间、业务有效时间、例外、映射、验证样例、批准依据、来源以及发布和观察计划。首份批准版本为 v1；可执行表示规则和映射可被受控能力校验或消费，不表示已发布或已在下游生效。

### Evidence

统一描述来源、读取时间、业务时间、新鲜度、权限、覆盖、摘要、支持/反对关系、未知和 Trace 引用。证据不是自动裁决。

### Human Microtask

描述交给特定角色的最小补证、确认、异议、审批、发布或结果解释任务，包含授权依据、最小上下文、依赖版本、截止、委托、提醒、升级和完成标准。

### Impact

描述候选规范对数据资产、指标、报表、模型、Agent、流程、策略、人群和目标系统的已知与候选影响，并保留来源、覆盖和不确定性。创建比较候选方案及可观察现状，修订比较绑定旧版；缺少历史基线时不伪造旧版差异或效果。

### Release

描述 Contract 版本到一个下游目标的 `compile`、`diff`、`validate`、`authorize`、`publish`、`verify`、`rollback` 和 `observe`，每个目标独立记录状态。

### Outcome Signal

保存采用、质量、异常、人工反馈和业务结果信号，包含口径、来源、窗口、基线和混杂因素；它不自动构成因果结论。

## 当前开发路径

### 路径一：产品定位、品牌与最小闭环 Decision Gate

- 英文产品与仓库身份使用 ContextOx；AlphaOx 退为历史工程名称。GitHub 仓库重命名已另行批准并完成；中文“数契”继续作为工作品牌，本地目录、既有包名、域名和法律/商标变更另行批准。
- 固定“从零建立电商高潜客户识别与运营优先级规范”的模拟任务、自有 fixture、单个发起人、单个明确批准者和 Contract v1 交付范围，仅面向已有购买记录的客户。
- 在 `docs/contextox/最小闭环-v0.1.md` 冻结首个闭环的起点、终点、状态机、审批绑定、复用边界、失败语义、非目标和验收条件。
- 首个闭环只证明 Agent 能形成可审阅提案、可靠等待人工决定、按决定恢复并形成可追溯 Contract v1；不把它写成真实客户价值、生产发布、多用户权限或跨日分布式运行已验证。
- 代码实施、真实模型/数据访问和外部写入分别批准；GitHub 仓库重命名已完成，路线图通过本身不授权其他远端动作。

### 路径二：最小领域状态与本地持久化

- 首版只实现 Mission、ApprovalRequest、Decision 和 ContractVersion；一个 Mission 暂时对应一个 Definition Change Case，不先建立通用工作流框架。
- 用 ContextOx 自有本地数据库保存业务状态；Pi Session 的分支、压缩、删除或重放不得改变审批与 Contract 事实。
- ApprovalRequest 冻结提案版本、内容哈希、范围、证据引用、未知项、影响摘要、所需批准者和创建时间；任何关键内容变化都使旧请求失效。
- Decision 绑定请求、预期提案版本与哈希、actor、结果、理由和时间；相同决定重放幂等，冲突决定或过期版本 fail closed。
- 只有有效 `approved` 决定能在同一事务中创建不可变 Contract v1；没有批准、拒绝、要求修改、权限不符或持久化失败均不得生成 Contract。
- 首轮覆盖创建 happy path、重启恢复、重复决定、冲突决定、提案失效、拒绝、要求修改和持久化失败；完整修订、发布、观察和多用户并发后置。

### 路径三：ContextOx TUI 人工审批闭环

- `packages/contextox` 提供 ContextOx 产品命令、identity、配置/状态路径与 Session 入口；内部复用 Pi Coding Agent TUI 的对话、过程和工具调用展示，不重写通用 Agent UI。
- ContextOx bootstrap 通过隔离 ResourceLoader 和内部 Pi adapter 注册 `request_human_approval`、`/contextox-review`、`/contextox-resume` 和审批卡片 renderer；只加载显式 ContextOx inline factory 与工具 allowlist，不发现项目/全局资源，不继承 Pi 默认文件、shell 或写入工具。
- Agent 从自有 fixture 形成候选规范、证据引用、未知项、样例/反例和影响摘要；工具先持久化冻结提案，再终止当前自动续跑并将 Mission 标记为 `waiting_for_human`。
- 人在 ContextOx TUI 中查看同一冻结版本，选择批准、拒绝或要求修改。actor 与 required approver 由 ContextOx 外层配置绑定，不来自 Pi flag、Session 或模型；决定必须先持久化。approve/request_changes 通过 ContextOx adapter 等待 runtime session 的恢复发送并持久化投递状态；reject 保持 blocked，不再触发 Agent turn。
- 批准后读取已创建的 Contract v1 并结束 Mission；要求修改时生成新提案版本和新审批请求；拒绝时保留明确终态或阻塞原因。
- 自动检查使用 fake provider 或确定性工具，验证 ContextOx identity、脱离 `.pi` 产品 extension 启动、Pi 默认行为回归、重启恢复、幂等、版本错配、状态丢失和错误保留；Agent 代行 TUI 验收、真实模型能力与人类验收分别记录。
- 首个闭环不发送外部通知、不跨用户分发任务、不发布到下游，也不以 Pi Session 记录代替领域审计。

### 路径四：后续全栈 Web、多用户与七分钟演示

- 只有路径三经自动检查和人类 TUI 验收后，才根据真实不足决定是否引入 Web、API、Worker、PostgreSQL 或持久工作流引擎；不得仅因原计划存在而实施。
- 使用 TypeScript 实现 Web、API 与 Worker 接缝；Web 状态来自真实领域事件，不用动画或预设步骤伪装自主执行。
- 演示“模糊目标 → 自主设计规范结构与候选方案 → 查证、冲突与责任发现 → 补证与自动恢复 → 核心业务裁决 → Contract v1、验证说明与发布提案”。成功条件满足后允许结束；观察/重开用独立标注的 fixture 演示，不假装已经发布。演示裁决次数不覆盖其他必要授权。
- 提供不强制旧 Contract 的目标入口、Mission 详情、待办和规范方案；创建展示候选比较与首份契约；首轮修订仅检查基线保护，完整旧/新对比在后续修订范围展示。Mission 执行流程显示真实行动、等待原因与下一步，Case Map 显示证据、分歧与裁决，引用同一批领域标识。
- 第一版流程图支持平移、缩放、筛选和详情定位，不提供拖线编辑执行逻辑；审批等动作由受控任务界面执行。权限、版本、新鲜度、未知、部分、错误与阻塞可见，并提供可访问的列表/文本等价表达。
- 数据来源、模拟业务任务/角色、fixture 回放、真实模型调用和外部效果分别标记；真实模型访问须独立授权和预算。UI 不把模拟任务称为客户案例，不把工程回放称为自主设计，不宣称生产发布或客户价值已验证。
- 在统一架构报告中提供完整内置 Browser 人工验收清单，覆盖精确 build/branch、起始状态、单个复用标签页、URL、viewport、zoom、测试数据、操作、预期、键鼠、焦点、滚动、响应式、加载/空/部分/错误/过期、可读性、console/network、reset 和结果记录。
- 自动验证后仍由人类记录最终视觉与功能验收。

### 路径五：沙箱受控发布与观察

- 将 AnalysisPlan、QueryPlan、preflight 和 EvidenceEnvelope 限定为证据发现与影响模拟能力；模拟输出范围、目标、差异和不确定性，不产生外部写入。
- 为一个可回滚沙箱目标实现 compile/diff/validate/authorize/publish/verify/rollback/observe 接缝；沙箱写入仍须目标级明确授权。
- 发布授权绑定 Case、Contract version、target、diff、actor、policy 和过期时间，关键版本或上下文变化后重新校验。
- 终端返回成功后必须 readback；部分发布、陈旧、回滚失败和原始错误保留，不以运行结束表示业务成功。
- 在授权范围内用观察信号提出或唤醒复审 Mission，不自动续用旧批准；沙箱回滚演练通过后才提出真实试点发布门。
- 演示身份不替代真实认证；试点前验证适用的身份、组织、委托和最小权限，企业 SSO 与生产运维在路径七进一步审议。

### 路径六：真实客户试点与结果验证

- 试点前建立定义完成周期、人工协调/中断次数、返工、证据完整度、下游一致性、单契约成本和不可复用 FDE 工时基线。
- 先跑一个专业用户发起、共 2–5 人参与的限定业务域，使用最少来源和一个获授权下游目标；在真实授权数据上复核检索质量与成本，不用演示数据结果代替试点结果。
- 将实现、自动验证、人工验收和业务结果分别标记。
- 未授权发布、批准/发布审计缺失必须为 0。
- 没有基线或测量设计时只报告功能闭环，不报告效率或因果收益。
- 只有第二个案例显示复用增加且 FDE 人工量下降后，才讨论规模化连接器、模板和商业交付。

### 路径七：仓库、Pi 上游与生产化决策

- GitHub 仓库已于 2026-09-01 经单独授权从 AlphaOx 重命名为 ContextOx，本地 origin 已同步更新；本地目录、包名、Git 历史重写、删除、可见性变化和 re-fork 仍未获授权。
- 完成当前 Pi/local 产品差异、根脚本、lockfile、CI、生成文件和历史的精确 inventory。
- 选择 ContextOx app 的完整历史、路径过滤历史或干净 baseline + 旧仓只读保留方案。
- 经单独远端授权建立/确认专用 Pi GitHub fork network；不把 ContextOx 产品代码放入 Pi fork。
- 在独立环境证明 app 使用 npm registry 的精确 Pi Agent Core 包，而不是 workspace source link。
- 完成依赖、provenance、SBOM、license、telemetry、Node runtime、漏洞和回滚审查。
- 完成企业身份、secret、日志脱敏、审计、备份、恢复、升级、灾备和部署 runbook。
- 未经独立批准不 re-fork、不创建/重命名/删除远端仓库、不迁移历史、不部署生产、不开放真实数据写回。

## 长期方向

- 支持更多 Definition Contract 类型、行业模板和组织联邦治理；在电商客户识别之后扩展完整修订、其他领域及有证据的预测用途，分别补齐业务事实与授权，不用订单金额自动替代未来潜力、意向或信用。
- 支持目录、指标层、数据契约、策略引擎、协作平台和 Agent context 的供应商中立适配器。
- 支持多目标分阶段发布、影响回归、版本兼容和目标级回滚。
- 支持定义采用、异常、漂移和业务结果反馈在授权范围内触发 Mission 复审与必要的人类裁决。
- 随真实案例扩展上下文来源和关系；复用必须保留适用范围、版本、证据与批准，单个案例结论不自动升级为组织级规范。
- 支持中国私有化/混合部署，并在有客户证据后验证海外市场。
- 支持授权后的 Contract、Context Resource 和 Tool 通过开放协议供其他 Agent 使用。

## 当前明确不做

- 不把 ChatBI、通用问数、任意 SQL 或任意代码执行作为产品主入口。
- 不替企业自动决定有争议的业务真相、例外或高风险发布。
- 不从零重建完整数据目录、血缘、指标、主数据、BI、IAM、通知或工作流平台。
- 不承诺任意企业零配置、零审核即可得到正确业务定义。
- 不以多 Agent、低代码画布、关系图或聊天 UI 本身作为差异化。
- 不把模型推断、字段名匹配、向量检索或第三方营销声明视为已批准事实。
- 不把原始企业数据、凭据、用户历史或受限第三方数据提交 Git、默认打入镜像或发送到未批准模型/遥测后端。
- 不在没有基线和方法时宣称业务因果收益。
- 不在未批准时推送、创建 Pull Request、re-fork、创建/删除/重命名仓库、部署、发布、生产切换、真实数据访问或外部写回。

## 人工决策门与分支

- 路线图只在默认分支 `main` 保存；任何编辑都必须先展示精确 diff 并取得该次修改的明确批准。
- 产品、品牌、PRD、架构、实现、自动验证、人工 Browser 验收、试点结果和生产决策分别批准，不互相替代。
- 架构决策由主 Agent 与人类互动完成；必须在项目权威位置形成包含替代方案、权衡、状态/失败矩阵、边界、依赖、迁移/回滚、完整验收清单、风险和批准状态的报告。
- Roadmap-only 或小型低风险变更可在精确批准后直接落到 `main`。
- 代码或高风险工作根据范围从当前 `main` 创建明确的 `codex/alphaox/pass-<n>`，必要时再创建 `codex/alphaox/pass-<n>-<slice>`；Pass 分支本身是该 Pass 的集成分支。
- 共享 checkout 中默认串行；分支、文件和 Owner 不清楚时停止，不清理、stash、reset 或覆盖其他会话工作。
- 在合并、release 或部署前展示精确 diff、验证、对抗审查、未解决风险和回滚边界，并等待对应人类 Decision Gate。
- 本路线图不授权 commit、merge、push、Pull Request、发布、部署、生产切换、真实数据访问、破坏性迁移或外部写回；这些操作必须分别获得明确授权。

## 当前讨论输入；不是自动权威

- `docs/contextox/产品方向与同类产品.md`
- `docs/contextox/产品定义与品牌核心.md`
- `docs/contextox/开源项目与能力复用清单.md`
- `docs/contextox/PRD-v0.1.md`
- `docs/contextox/产品形态与执行架构决策-v0.1.md`
- `docs/contextox/Pi内核与上游同步策略.md`
- [Uber：Running a Software Factory Efficiently at Uber Scale](https://www.uber.com/us/en/blog/efficient-software-factory/)（上下文与托管 Agent 案例，不作为本项目性能承诺或完整实现依据）

这些文档中的 `DRAFT`、`PENDING`、`DEMO NEEDED`、产品假设和架构候选只有在人类逐项批准后，才进入本路线图或后续权威架构。
